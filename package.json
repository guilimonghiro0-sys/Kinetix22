{
  "name": "kinetix-site",
  "version": "1.0.0",
  "private": true,
  "description": "Site vitrine Kinetix Agency — formulaire de contact via Resend (Vercel Serverless)",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "resend": "^4.0.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@vercel/node": "^3.2.24",
    "typescript": "^5.6.3"
  }
}
